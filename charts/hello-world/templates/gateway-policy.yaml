{{- if and .Values.httpRoute.enabled .Values.httpRoute.allowGatewayTraffic }}
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: {{ include "hello-world.fullname" . }}-gateway
  labels:
    {{- include "hello-world.labels" . | nindent 4 }}
spec:
  endpointSelector:
    matchLabels:
      {{- include "hello-world.selectorLabels" . | nindent 6 }}
  ingress:
    - fromEntities:
        - cluster
        - host
        - remote-node
      toPorts:
        - ports:
            - port: {{ .Values.httpRoute.gatewayTrafficPort | default 8080 | quote }}
              protocol: TCP
{{- end }}
